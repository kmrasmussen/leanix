# Roadmap Next Ticket Wave

The roadmap has been refreshed after the completed `TICKET-0033` state.

The old ticket-wave file described `TICKET-0026` through `TICKET-0033`, all of
which are now complete. The new wave is `TICKET-0034` through `TICKET-0044`.
It focuses on the remaining pressure in the project: service schemas, schema
catalog docs, deeper build-plan constructors and validation, Rust-owned
artifact verification, multiple artifact examples, checked output evidence,
topological package closure checking, artifact interop with nixparserlean,
focused CLI checking, and a clearer policy matrix for CI and impure sources.

The current-state and milestone roadmap docs were updated at the same time so
they describe the codebase as it stands now rather than the older post-0025
snapshot. The new tickets are intentionally analysis-ready: each has a problem,
goal, scope, acceptance criteria, verification path, and suggested files.
